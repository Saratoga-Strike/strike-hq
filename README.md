# Strike HQ — GoHighLevel-Ready Version

Included updates:
- HQ remains intentionally different from the four role cards
- centralized CSS variables
- reusable accent/button styling
- one JavaScript configuration section for HighLevel Media Storage URLs
- permanent GoHighLevel page paths
- matching red SVG icons instead of emojis
- Google Fonts: Orbitron + Inter
- lazy loading on portal icon images
- subtle footer
- clearly organized Why Strike copy blocks
- existing Strike HQ external link preserved

Files:
- index.html
- css/styles.css
- js/app.js
- strike-homepage-reference.html (single-file reference/preview)

Still requires your input:
1. HighLevel Media Storage URLs must be pasted into STRIKE_CONFIG.assets in js/app.js.
2. The favicon file must be uploaded to HighLevel. Additional 16x16/32x32 variants can be made once the final favicon source is available.
3. GoHighLevel may require the HTML, CSS, and JS to be pasted into separate editor areas. The single-file HTML is provided as a reference, while the three-file version is the safer import structure.
